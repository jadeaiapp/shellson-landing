{
  "name": "shellson-landing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Shellson için bağımsız konsept landing page — resmî web sitesi değildir.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "images": "node tools/build-images.mjs",
    "fonts": "node tools/fetch-fonts.mjs",
    "check": "node tools/check.mjs",
    "verify": "npm run typecheck && npm run build && npm run check"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "playwright": "^1.62.1",
    "sharp": "^0.34.5",
    "typescript": "^5.9.3",
    "vite": "^7.3.6"
  }
}
